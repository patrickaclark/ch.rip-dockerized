# Path to your audiobook library
# This is where downloaded books will be saved
AUDIOBOOKS_PATH=/path/to/your/audiobooks

# Optional: set a VNC password for the noVNC interface
# Leave blank for no authentication (fine for private networks)
VNC_PASSWORD=
